using UnityEngine;
using System.Collections;

public class Block {

    // Declare public block variabls color, position
    public Color blockColor = new Color(0, 0, 0);
    public Vector2 blockPos = new Vector2(0, 0);
    
    // Declare size variable, default 4
    public int size = 4;
}
